VBoxHypervisor (ALPHA)
--------------


This is a simple module to configure a Virtualbox hypervisor.
It installs needed packages and a vboxmgr user.
It still contains hardcoded paths and such, inspect befire using.